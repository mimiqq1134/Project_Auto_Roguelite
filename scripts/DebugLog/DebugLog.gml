// Script assets have changed for v2.3.0 see
// https://help.yoyogames.com/hc/en-us/articles/360005277377 for more information
/// @function DebugLog(m_message);
/// @param {string} m_message The debug message to output

function DebugLog(m_message) {
	//if (date_get_timezone() != timezone_utc) {
	//	date_set_timezone(timezone_utc);
    //}
	//var m_date = string(current_year) + "/" + DoubleDigitToString (current_month) + "/" + DoubleDigitToString(current_day),
	//m_time = DoubleDigitToString(current_hour) + ":" + DoubleDigitToString (current_minute) + ":" + DoubleDigitToString(current_second),
	//m_debugLog = "(UTC), " + m_date + " " + m_time + ", " + object_get_name(object_index) + " - " + m_message;
	//if (debug_mode) {
	//	show_debug_message(m_debugLog);
	//}
	//var m_log_file = file_text_open_append(working_directory + "debug.log");
	//file_text_write_string(m_log_file, m_debugLog + "\n");
	//file_text_close(m_log_file);
}